# CI Workflow - Automated Testing and Quality Checks
#
# This workflow runs on every push and pull request to ensure code quality.
# It performs:
# - Frontend linting (ESLint + Prettier)
# - Frontend unit tests (Jest) with coverage
# - Frontend build (TypeScript check + Next.js build)
# - Backend tests (JUnit) with JaCoCo coverage
#
# Workflow Structure:
# ┌─────────────────────────────────────────────────────────┐
# │                    Push / PR Trigger                     │
# └─────────────────────────┬───────────────────────────────┘
#                           │
#       ┌───────────────────┼───────────────────┐
#       │                   │                   │
#       ▼                   ▼                   ▼
# ┌──────────┐        ┌──────────┐        ┌──────────┐
# │ Frontend │        │ Frontend │        │ Backend  │
# │   Lint   │        │  Tests   │        │  Tests   │
# │(ESLint + │        │ (Jest +  │        │(JUnit +  │
# │ Prettier)│        │ Coverage)│        │ JaCoCo)  │
# └──────────┘        └──────────┘        └──────────┘
#       │                   │                   │
#       └─────────┬─────────┴───────────────────┘
#                 │
#                 ▼
#          ┌──────────┐
#          │ Frontend │
#          │  Build   │
#          │(TypeScript)
#          └──────────┘
#                 │
#                 ▼
#          All Pass = ✅
#          Any Fail = ❌
#

name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Cancel in-progress runs when a new run is triggered
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: '20'
  JAVA_VERSION: '21'

jobs:
  # ===========================================
  # Frontend Lint (ESLint + Prettier)
  # ===========================================
  frontend-lint:
    name: Frontend Lint
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install native binaries for Linux
        run: |
          cd frontend
          npm install @next/swc-linux-x64-gnu @next/swc-linux-x64-musl @unrs/resolver-binding-linux-x64-gnu --no-save --force || true

      - name: Run ESLint
        working-directory: frontend
        run: npm run lint:check

      - name: Check Prettier formatting
        working-directory: frontend
        run: npm run format:check

  # ===========================================
  # Frontend Unit Tests (Jest + Coverage)
  # ===========================================
  frontend-tests:
    name: Frontend Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install native binaries for Linux
        run: |
          cd frontend
          npm install @next/swc-linux-x64-gnu @next/swc-linux-x64-musl @unrs/resolver-binding-linux-x64-gnu --no-save --force || true

      - name: Run frontend unit tests
        working-directory: frontend
        run: npx jest --watchAll=false --coverage

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: frontend-coverage
          path: |
            coverage/
            frontend/coverage/
          retention-days: 7
          if-no-files-found: warn

      - name: Coverage Summary
        run: |
          echo "## Frontend Coverage Report" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ -f coverage/lcov.info ] || [ -f frontend/coverage/lcov.info ]; then
            echo "✅ Jest coverage report generated successfully" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "📊 Coverage report uploaded as artifact" >> $GITHUB_STEP_SUMMARY
          else
            echo "✅ Tests completed" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "📊 Check the uploaded artifact for detailed coverage" >> $GITHUB_STEP_SUMMARY
          fi

  # ===========================================
  # Frontend Build (TypeScript + Next.js)
  # ===========================================
  frontend-build:
    name: Frontend Build
    runs-on: ubuntu-latest
    needs: [frontend-lint, frontend-tests]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install native binaries for Linux
        run: |
          cd frontend
          npm install @next/swc-linux-x64-gnu @next/swc-linux-x64-musl @unrs/resolver-binding-linux-x64-gnu --no-save --force || true

      - name: Build frontend
        run: npm run build:frontend

  # ===========================================
  # Backend Tests (JUnit + JaCoCo Coverage)
  # Note: Tests use H2 in-memory database (configured in application-test.properties)
  # PostgreSQL service not needed for unit tests
  # ===========================================
  backend-tests:
    name: Backend Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: ${{ env.JAVA_VERSION }}
          cache: 'maven'

      - name: Cache Maven packages
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Run backend tests with coverage
        working-directory: backend/ikp-labs-api
        # Tests use H2 in-memory database (configured in application-test.properties)
        # Test exclusions configured in pom.xml surefire plugin
        # TODO: Fix pre-existing failing tests and remove exclusions from pom.xml
        run: mvn test -B

      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: backend-test-results
          path: backend/ikp-labs-api/target/surefire-reports/
          retention-days: 7

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: backend-coverage
          path: backend/ikp-labs-api/target/site/jacoco/
          retention-days: 7

      - name: Coverage Summary
        working-directory: backend/ikp-labs-api
        run: |
          echo "## Backend Coverage Report" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ -f target/site/jacoco/index.html ]; then
            echo "✅ JaCoCo coverage report generated successfully" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "📊 Coverage report uploaded as artifact" >> $GITHUB_STEP_SUMMARY
          else
            echo "⚠️ Coverage report not found" >> $GITHUB_STEP_SUMMARY
          fi

  # ===========================================
  # Summary Job - All checks must pass
  # ===========================================
  ci-success:
    name: CI Summary
    runs-on: ubuntu-latest
    needs: [frontend-lint, frontend-tests, frontend-build, backend-tests]
    if: always()

    steps:
      - name: Check all jobs status
        run: |
          if [[ "${{ needs.frontend-lint.result }}" != "success" ]] || \
             [[ "${{ needs.frontend-tests.result }}" != "success" ]] || \
             [[ "${{ needs.frontend-build.result }}" != "success" ]] || \
             [[ "${{ needs.backend-tests.result }}" != "success" ]]; then
            echo "❌ CI Failed!"
            echo "- Frontend Lint: ${{ needs.frontend-lint.result }}"
            echo "- Frontend Tests: ${{ needs.frontend-tests.result }}"
            echo "- Frontend Build: ${{ needs.frontend-build.result }}"
            echo "- Backend Tests: ${{ needs.backend-tests.result }}"
            exit 1
          fi
          echo "✅ All CI checks passed!"
